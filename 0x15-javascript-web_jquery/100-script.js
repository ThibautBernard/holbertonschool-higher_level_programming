/**
  change font color of header element when the dom is 'ready'
**/
document.addEventListener('DOMContentLoaded', (evt) => {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
});
